<div class="container">
  <div class="row"> 
    <div class="responsive">
      <div class="container col-sm-12 col-lg-12"
        <h1 class="left" style="margin-bottom:25pt display: inline-block">My Profile</h1>

              <p>Student Number: {{this.student_number}}</p>
              <p>Name: {{this.first_name}} {{this.last_name}}</p>
              <p>Email Address: {{this.email}}</p>
              <p>Phone: {{this.phone}}</p>

       </div>
    </div>
  </div>
</div>